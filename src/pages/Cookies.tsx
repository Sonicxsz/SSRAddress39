import Layout from '@/layout/layout';
import { Cookies } from '@/components/screen/terms/Cookies';




export default function TermsPage() {
    return (
        <Layout>
            <Cookies/>
        </Layout>
    );
}